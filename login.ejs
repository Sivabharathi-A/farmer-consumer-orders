
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Farmer & Consumer Login</title>
    <link rel="stylesheet" href="/style.css">
    <script src="/script.js" defer></script>
</head>
<style>
    @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap');

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Poppins', sans-serif;
}

body {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    background-image: url('https://png.pngtree.com/background/20230425/original/pngtree-produce-at-a-local-farmers-market-picture-image_2476446.jpg'); /* Change to your image */
    background-size: cover;
    perspective: 1000px;
}

/* Flip Box Container */
.container {
    width: 400px;
    height: 500px;
    position: relative;
}

.flip-box {
    width: 100%;
    height: 100%;
    position: relative;
    transform-style: preserve-3d;
    transition: transform 0.6s ease-in-out;
}

.flip-box-inner {
    width: 100%;
    height: 100%;
    position: absolute;
    transform-style: preserve-3d;
}

.flip-box-front, .flip-box-back {
    width: 100%;
    height: 100%;
    position: absolute;
    backface-visibility: hidden;
    border-radius: 15px;
    padding: 30px;
    display: flex;
    color: white;
    flex-direction: column;
    justify-content: center;
    backdrop-filter: blur(10px);                 /* blur effect */
    -webkit-backdrop-filter: blur(10px);         /* Safari support */
    border: 2px solid black;                     /* black border */
    padding: 40px;
    border-radius: 15px;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);   /* optional shadow */
    width: 400px;
    text-align: center;
}

/* Farmer Login */
.flip-box-front {
    background: rgba(255, 255, 255, 0.1); 
}

/* Consumer Login */
.flip-box-back {
    background: rgba(255, 255, 255, 0.1); 
    transform: rotateY(180deg);
}

/* Input Fields */
form input {
    width: 100%;
    padding: 12px;
    margin: 10px 0;
    border: none;
    border-radius: 8px;
    font-size: 16px;
}

/* Login & Signup Buttons */
button {
    width: 100%;
    padding: 12px;
    background: #ff9f43;
    border: none;
    border-radius: 8px;
    color: white;
    font-size: 18px;
    cursor: pointer;
    transition: background 0.3s ease;
    font-weight: bold;
}

button:hover {
    background: #ff6f00;
}

/* Toggle Button */
.toggle-btn {
    background: rgba(255, 255, 255, 0.3);
    border: none;
    padding: 10px;
    font-size: 14px;
    cursor: pointer;
    border-radius: 5px;
    margin-top: 10px;
    color: white;
    transition: background 0.3s;
}

.toggle-btn:hover {
    background: rgba(255, 255, 255, 0.6);
}

/* Sign Up Link */
p {
    margin-top: 15px;
    color: white;
    font-size: 14px;
}

p a {
    color: yellow;
    font-weight: bold;
    text-decoration: none;
    transition: color 0.3s;
}

p a:hover {
    color: #ffeb3b;
}

</style>
<body>
    <div class="container">
        <div class="flip-box" id="flipBox">
            <div class="flip-box-inner">
                
                <!-- Farmer Login -->
                <div class="flip-box-front farmer">
                    <h2>Farmer Login</h2>
                    <form action="/login" method="post">
                        <input type="hidden" name="userType" value="Farmer">
                        <input type="text" name="username" placeholder="Farmer ID" required>
                        <input type="password" name="password" placeholder="Password" required>
                        <button type="submit">Login</button>
                    </form>
                    <p>Don't have an account? <a href="/signup">Sign Up</a></p>
                    <button class="toggle-btn" onclick="flipCard()">Consumer Login</button>
                </div>

                <!-- Consumer Login -->
                <div class="flip-box-back consumer">
                    <h2>Consumer Login</h2>
                    <form action="/login" method="post">
                        <input type="hidden" name="userType" value="Consumer">
                        <input type="text" name="username" placeholder="Consumer ID" required>
                        <input type="password" name="password" placeholder="Password" required>
                        <button type="submit">Login</button>
                    </form>
                    <p>Don't have an account? <a href="/signup">Sign Up</a></p>
                    <button class="toggle-btn" onclick="flipCard()">Farmer Login</button>
                </div>

            </div>
        </div>
    </div>
</body>
<script>
    document.addEventListener("DOMContentLoaded", function () {
    const flipBox = document.getElementById("flipBox");

    window.flipCard = function () {
        if (flipBox.style.transform === "rotateY(180deg)") {
            flipBox.style.transform = "rotateY(0deg)";
        } else {
            flipBox.style.transform = "rotateY(180deg)";
        }
    };
});

</script>
</html>

